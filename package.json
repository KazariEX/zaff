{
  "private": true,
  "version": "1.3.1",
  "type": "module",
  "packageManager": "pnpm@9.5.0",
  "scripts": {
    "build": "pnpm -r build && tsx scripts/copy-files.ts",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "dev": "pnpm -r dev",
    "lint": "eslint src/**/*.ts",
    "release": "bumpp -r --no-push --commit \"release: v%s\"",
    "test": "vitest"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.21.3",
    "@types/node": "^20.14.10",
    "@zinkawaii/eslint-config": "^0.1.1",
    "bumpp": "^9.4.1",
    "eslint": "^9.6.0",
    "tsup": "^8.1.0",
    "tsx": "^4.16.2",
    "vitepress": "1.3.0",
    "vitest": "^2.0.1"
  }
}
